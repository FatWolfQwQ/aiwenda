# 项目分块维护说明

## 维护原则

只改当前功能所属文件，不跨模块混改。公共工具放 `app-core.js` 或 `server/modules/*`，业务逻辑放对应模块。

## 前端结构

`public/index.html` 只负责页面结构，并已按功能区写入 `MODULE` 标记：

- `Auth Gate`：登录 / 注册入口。
- `Home`：首页展示、注意事项、使用说明。
- `Knowledge Base Create`：创建知识库。
- `Document Upload`：上传文档。
- `Knowledge Base List`：知识库列表。
- `Document List`：文档列表。
- `AI Chat`：AI 问答主界面、选择知识库 / 文档弹窗、聊天记录弹窗。
- `Logs`：管理员问答日志。
- `Admin Users`：管理员用户管理。
- `User Center`：头像设置、修改密码。
- `Global Busy Overlay`：全局请稍等弹窗。

## 前端 JS 模块

加载顺序固定为：`app-core.js -> auth.js -> knowledge-documents.js -> chat.js -> logs.js -> boot.js`。

- `public/js/app-core.js`：DOM 引用、全局状态、请求、通用工具、头像渲染、聊天公共渲染。
- `public/js/auth.js`：登录、注册、权限显示、用户中心、管理员用户管理。
- `public/js/knowledge-documents.js`：创建知识库、刷新知识库、选择知识库、删除知识库、上传文档、文档列表、打开/删除文档。
- `public/js/chat.js`：AI 问答、问答设置弹窗、文档范围选择、回答模式、聊天记录、资料依据展示。
- `public/js/logs.js`：日志列表、日志详情、删除日志记录、导出日志、打开日志位置。
- `public/js/boot.js`：页面启动、健康检查、标签页切换、刷新按钮、路径选择入口。

## 样式模块

`public/styles.css` 只引入 CSS 分块，不直接写业务样式。

- `public/css/01-base-layout.css`：基础布局、表单、按钮、卡片、聊天/日志基础样式。
- `public/css/02-non-chat-upgrade.css`：非 AI 模块的高级 UI 覆盖。
- `public/css/03-premium-modules.css`：首页、知识库、文档、日志等模块增强样式。
- `public/css/04-ai-chat-flow.css`：AI 问答流程和聊天区域基础样式。
- `public/css/05-chat-modal-preview.css`：AI 弹窗、像素聊天框、等待卡片、回复提醒。

## 后端结构

`server/index.js` 仍是 HTTP 入口，已按 `REGION` 分区：

- `HTTP Utilities`：JSON 响应、请求体读取。
- `Native Windows Helpers`：选择文件夹、打开资源管理器、PowerShell 调用。
- `Auth Helpers`：密码哈希、用户公开字段、头像校验。
- `File And Name Helpers`：文件名、文件夹名、支持格式校验。
- `Text Decoding And Cleanup`：编码识别、乱码清洗、HTML/RTF 文本提取。
- `Office Document Extraction`：DOCX、PPTX、XLSX 解析。
- `PDF And OCR Extraction`：PDF 文本解析、PDF/图片 OCR。
- `Storage Path Resolution`：知识库、文档、日志路径解析。
- `Local Vectorization And Retrieval`：切片、本地向量、并行检索。
- `DeepSeek Chat Integration`：DeepSeek 调用、问答 Prompt、引用分析。
- `Log File Helpers`：日志标题、日志分级、日志导出文件。
- `Static File Server`：静态文件服务。
- `API Routes`：所有 API 路由。
- `Server Startup`：服务启动。

已拆出的后端模块：

- `server/modules/paths.js`：项目路径、端口、脚本路径。
- `server/modules/db.js`：`data/db.json` 读写。
- `server/modules/chunk-search-worker.js`：大文档检索 Worker。
- `scripts/ocr-document.ps1`：PDF / 图片 OCR。

## 修改范围对照

- 改登录/用户中心：只动 `auth.js`，必要时改后端 Auth/Admin 路由。
- 改知识库/文档：只动 `knowledge-documents.js`，必要时改后端 Knowledge/Documents 路由。
- 改 AI 问答：只动 `chat.js`，必要时改后端 Chat/DeepSeek/Retrieval 区域。
- 改日志：只动 `logs.js`，必要时改后端 Logs 区域。
- 改首页文案：只动 `index.html` 的 `Home` 区域。
- 改样式：先判断模块，再改对应 CSS 文件。
- 新增公共工具：前端放 `app-core.js`，后端优先新建 `server/modules/*`。
